describe Fastlane::Actions::AuthWithGithubAppAction do
  describe '#run' do
    it 'prints a message' do
      # expect(Fastlane::UI).to receive(:message).with("The auth_with_github_app plugin is working!")

      # Fastlane::Actions::AuthWithGithubAppAction.run(nil)
    end
  end
end
